<form method="post" action="{{ path('bill_delete', {'billId': bill.billId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bill.billId) }}">
    <button class="btn">Delete</button>
</form>
