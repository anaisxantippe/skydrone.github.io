<form method="post" action="{{ path('suppliers_delete', {'suppliersId': supplier.suppliersId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ supplier.suppliersId) }}">
    <button class="btn">Delete</button>
</form>
