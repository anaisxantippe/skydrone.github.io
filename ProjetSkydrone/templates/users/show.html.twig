{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <h1>Utilisateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID Utilisateur</th>
                <td>{{ user.userId }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ user.role }}</td>
            </tr>
            <tr>
                <th>Mot de passe</th>
                <td>{{ user.password}}</td>
            </tr>
            <tr>
                <th>Identifiant</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('users_index') }}">Retour à la liste</a>

    <a href="{{ path('users_edit', {'userId': user.userId}) }}">Modifier</a>

    {{ include('users/_delete_form.html.twig') }}
{% endblock %}
