<form method="post" action="{{ path('opinion_delete', {'opinionId': opinion.opinionId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ opinion.opinionId) }}">
    <button class="btn btn-dark">Supprimer</button>
</form>
