<div class="container-fluid">
    {{ form_start(form) }}
    <div class="form-group">{{ form_row(form.title) }}</div>
    <div class="form-group">{{ form_row(form.description) }}</div>
    <div class="form-group">{{ form_row(form.product) }}</div>
    <div class="form-group">{{ form_row(form.product) }}</div>
    <div class="form_group">{{ form_row(form.customer) }}</div>
    <br>
    <button class=" btn btn-success">{{ button_label|default('Ajouter l\'avis') }}
        {{ form_end(form) }} </button>
</div>



