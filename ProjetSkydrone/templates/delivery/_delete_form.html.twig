<form method="post" action="{{ path('delivery_delete', {'deliveryId': delivery_form.deliveryId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_form.deliveryId) }}">
    <button class="btn">Delete</button>
</form>
