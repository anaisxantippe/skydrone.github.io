{% extends 'base.html.twig' %}

{% block title %}DeliveryForm{% endblock %}

{% block body %}
    <h1>DeliveryForm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>DeliveryId</th>
                <td>{{ delivery_form.deliveryId }}</td>
            </tr>
            <tr>
                <th>DeliveryDate</th>
                <td>{{ delivery_form.deliveryDate ? delivery_form.deliveryDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('delivery_index') }}">back to list</a>

    <a href="{{ path('delivery_edit', {'deliveryId': delivery_form.deliveryId}) }}">edit</a>

    {{ include('delivery/_delete_form.html.twig') }}
{% endblock %}
