<form method="post" action="{{ path('executives_delete', {'executivesId': executive.executivesId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ executive.executivesId) }}">
    <button class="btn">Supprimer</button>
</form>
