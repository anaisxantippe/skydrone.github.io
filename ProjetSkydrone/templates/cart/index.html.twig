{% extends 'base.html.twig' %}

{% block title %}Hello CatalogController!{% endblock %}

{% block body %}
    {%  %}
    {%  %}
    {%  %}
    <table class="table">

        <thead>
        <tr>
            <th scope="col">ID du produit</th>
            <th scope="col">Nom</th>
            <th scope="col">Description</th>
            <th scope="col">Prix HT</th>
            <th scope="col">Photo</th>
        </tr>
        </thead>
        <tbody>

    <h1>Votre panier</h1>

    {% if items | length > 0 %}
        <table class="table">

            <thead>
            <tr>
                <th scope="col">ID du produit</th>
                <th scope="col">Photo</th>
                <th scope="col">Nom du produit</th>
                <th scope="col">Prix unitaire HT</th>
                <th scope="col">Quantité</th>
                <th scope="col">Prix total HT</th>
                <th scope="col"></th>
            </tr>
            </thead>

            <tbody>
            {% for item in items %}
            <tr>
                <th scope="row"></th>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <th scope="row">{{ item.product.productID }}</th>
                <td>{{ item.product.picture }}</td>
                <td>{{ item.product.productName }}</td>
                <td>{{ item.product.dfPrice }}</td>
                <td>{{ item.quantity }}</td>
                <td>{{ item.product.dfPrice * item.quantity }}</td>
                <td><a href="{{ path('cart_remove', {'id' : item.product.productId}) }}" class="btn btn-danger"><i class="fas fa-trash-alt"></i></a></td>
            </tr>
        </tbody>
    </table>
            {% endfor %}
            </tbody>

            <tbody>
                <tfoot>
                    <tr>
                        <td colspan="5" class="text-end">Total :</td>
                        <td>{{ total }}</td>
                        <td></td>
                    </tr>
                </tfoot>
            </tbody>

        </table>
    {% else %}
        <p>Votre panier est vide :'(</p>
    {% endif %}
{% endblock %}
